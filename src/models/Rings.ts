export default class Rings {
    amount?: number
    diameter?: any
    radius?: any
    lineWidth?: any

    public initializeValues(amount, diameter, radius, lineWidth){
        this.amount = amount
        this.diameter = diameter
        this.radius = radius
        this.lineWidth = lineWidth
    }
}